def studentcheck(slotexamschedule, studentcourse):
    templist = slotexamschedule
    flag = False
    x = 0
    for i in templist:
        for j in templist:
            if i[len(i)-1] == j[len(j)-1]:
                if i[0] and j[0] in studentcourse:
                    flag = True
    return flag


def costfunction(examsch):
    cost = 0
    st = groupcourses()
    key = list(st.keys())
    for k in examsch:
        for i in key:
            if studentcheck(k, st[i]):
                cost = cost + 1
    return cost